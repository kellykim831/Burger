<h1>Eat Da Burger!</h1>

<h2>Which burger are you craving today?</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burger-block devour=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers you've devoured</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burger-block devour=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="slee">Would you like to devour this burger?</label><br>
    <input type="radio" name="devour" value="1" checked> Devour it!<br>
    <input type="radio" name="devour" value="0"> Eww No Thanks
  </div>

  <button type="submit">Add Burger</button>
</form>
